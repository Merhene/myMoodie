{% extends 'base.html.twig' %}

{% block title %}Mood
{% endblock %}

{% block body %}
	<h1>Mood</h1>
<div class="card-container d-flex flex-column">
	<table class="table">
		<tbody>
{% if mood.type == 'happy' %}
					<tr>
						<a href="{{ path('app_mood_show', {'id': mood.id}) }}" class="btn btn-success">😊
						</a>
					</tr>
				{% elseif mood.type == 'septic' %}
					<tr>
						<a href="{{ path('app_mood_show', {'id': mood.id}) }}" class="btn btn-warning">😶
						</a>
					</tr>
				{% elseif mood.type == 'angry' %}
					<tr>
						<a href="{{ path('app_mood_show', {'id': mood.id}) }}" class="btn btn-danger">😠
						</a>
					</tr>
				{% else %}
					<tr>
						<a href="{{ path('app_mood_show', {'id': mood.id}) }}" class="btn btn-info">😢
						</a>
					</tr>
                    {% endif %}
			<td>{{ mood.description }}</td>
			{# <td>{{ mood.date ? mood.date|date('d-m-Y H:i') : '' }}</td> #}
		</tbody>
	</table>
    </div>
	<div class="card-container d-flex flex-column">
		<a href="{{ path('app_mood_edit', {'id': mood.id}) }}" class="btn btn-outline-secondary">edit</a>
		<a href="{{ path('app_mood_index') }}" class="btn btn-outline-secondary">back</a>
		<a href="{{ path('app_mood_edit', {'id': mood.id}) }}" class="btn btn-outline-secondary">{{ include('mood/_delete_form.html.twig') }}</a>
	</div>

{% endblock %}
